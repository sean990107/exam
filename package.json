{
  "name": "exam-system",
  "version": "1.0.0",
  "description": "卡片式考试系统",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd client && npm install --production=false && cd ../server && npm install --production=false",
    "build:client": "cd client && react-scripts build",
    "build:server": "cd server && tsc",
    "copy-client": "mkdir -p server/dist/public && cp -r client/build/* server/dist/public/ || true",
    "build": "npm run build:client && npm run build:server && npm run copy-client",
    "start:prod": "cd server && node dist/index.js",
    "start": "cd server && node dist/index.js"
  },
  "keywords": [
    "exam",
    "react",
    "nodejs",
    "typescript",
    "antd"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
} 